void main() {
  var name = '경준';
  // 관습적으로 함수나 메소드 내부에 지역 변수를 선언할때는 var 사용
  
  // == String name = '경준';
  // 클래스에서 변수나 property를 선언할 때는 타입을 지정

  /*name = 1; 
  -> name은 String 변수이므로 int값으로 변경 불가
  데이터의 형식만 맞춘다면 업데이트 가능
  */
  name = 'Jun';
}